syntax = "proto3";

package Chat;

message CS_ChatRoomRequest {
  uint64 roomId = 1;
}

message SC_ChatRoomResponse {
  uint64 roomId = 1;
  uint64 leader = 2;

  message ChatRoomMember {
    uint32 pid = 1;
    bool online = 2;
  }

  repeated ChatRoomMember memberList = 3;

  enum Reason {
    NORMAL_SEND = 0;
    MEMBER_ENTERED = 1;
    MEMBER_EXITED = 2;
  }

  Reason reason = 4;
}